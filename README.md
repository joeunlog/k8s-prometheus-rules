This chart is for deploying prometheus rules.  
This chart contain common rules for k8s.  

## dependency
- kube-prometheus-stack helm chart (for prometheusRule CRD)

# Install
`helm install k8s-rules .`